_target_: dem.energies.unbal_gmm_energy.GMM

dimensionality: 32
n_mixes: 8
loc_scaling: 8
log_var_scaling: 1.0

device: ${trainer.accelerator}

plot_samples_epoch_period: 1

should_unnormalize: true
data_normalization_factor: 10
data_path_train: null
